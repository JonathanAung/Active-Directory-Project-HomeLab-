#include <iostream>
#include <string>
#include "DetectionEngine.h"

int main() {
    std::cout << "\n==========================================\n";
    std::cout << "     BRUTE FORCE DETECTION SYSTEM\n";
    std::cout << "==========================================\n";

    // TODO: declare a string for the log filename and prompt the user to enter it

    // TODO: create a DetectionEngine object
    //       you can use the default threshold (5) or let the user choose

    // TODO: call loadAttempts() with the filename

    // TODO: call runAll() to run all detection checks

    return 0;
}
