#include <iostream>
#include <string>
#include "Target.h"
#include "Scanner.h"

int main() {
    std::cout << "\n==========================================\n";
    std::cout << "           C++ PORT SCANNER\n";
    std::cout << "==========================================\n";
    std::cout << "Note: only use this on networks you own or have permission to scan.\n\n";

    // TODO: declare strings for the IP address and ints for start/end port range

    // TODO: prompt the user to enter a target IP address

    // TODO: prompt the user to enter a start port and end port

    // TODO: create a Target object with the IP

    // TODO: create a Scanner object

    // TODO: call scanRange() on the scanner, passing in the target and port range

    // TODO: call printResults() on the target

    return 0;
}
